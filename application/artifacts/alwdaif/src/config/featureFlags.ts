/**
 * Central visibility switches. The features and their data remain in the codebase,
 * but their routes and navigation are hidden while these flags are disabled.
 */
export const FEATURE_FLAGS = {
  services: false,
  community: false,
} as const;

